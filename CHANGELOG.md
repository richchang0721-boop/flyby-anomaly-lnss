# CHANGELOG

記錄 `results.json`、`constants.py` 的每一次數值變動：日期、變了什麼、為什麼、誰發現的。

規則：任何數值變動都必須在這裡留一筆，不允許「直接改掉沒說明」。

---

## 2026-09-23 — 負面發現：潮汐項（第三層）與 ap 係數 c₂（第二層）確認需重驗

**起因：** 撰寫 `05a_JUICE_2026_Boundary_Conditions.md` 第 5.2、5.3 節時，原本打算直接引用「潮汐項改善 RMS 26%」與「c₂ = −0.249 mm/s/nT」作為 JUICE 飛掠的預先宣告輔助項。為了確認這兩個數字能否作為封存依據，回頭逐一追查其來源，發現兩者都**未經 2026-07-02 審計後的修正資料重驗**，且無法直接套用到 JUICE。

**發現 1：潮汐項（`02_Constraints.md`、`04_Mathematics.md`）**
- 26% 的出處是雙變量模型 `ΔV = 10.94 − 0.47·ap + 5.18×10¹³·T_proj`（RMS 3.99 → 2.94 mm/s）。
- 此模型擬合於審計之前，repo 中沒有以修正後資料重驗的紀錄。
- 潮汐投影 T_proj 單獨與 ΔV 的相關係數只有 r = +0.093，幾乎無相關；以 n = 7 擬合兩個變數，26% 的改善可能有相當部分來自過度擬合。

**發現 2：ap 係數 c₂（`04_Mathematics.md`、`Appendix_Data.md`、`02_Constraints_EN.md`）**
- c₂ = −0.249 的出處是 `ΔV = 3.80 + 0.579·ΔV_Anderson − 0.249·ap`。
- 此擬合使用審計前的 ΔV_Anderson，而 Juno 的 P₁ 預測已由 +10.4 修正為 +5.99～6.34 mm/s（見 2026-07-02 條目）。Juno 在 n = 7 裡是槓桿最大的案例，所以 c₂ 幾乎必然需要重新擬合。`04_Mathematics_EN.md` 表格本身也已註明 "RMS etc. pending refit with new angles"，但 c₂ 在其他文件中仍以確定值的形式出現。
- 兩個模型的 ap 係數不一致（−0.47 vs −0.249），而文件中「ap 為第二層訊號、c₂ = −0.249」的敘述沒有交代這個分歧。

**發現 3：兩個模型都不能當作 ΔV_P1 的修正項**
- 兩者都帶截距（10.94、3.80），是對整體 ΔV 的迴歸，不是疊加在 Anderson P₁ 上的小修正。
- 因此 05a 原草稿「c₂ 固定、不再調整」的寫法在操作上無法執行：沒有辦法把 c₂ 單獨拿出來，對 JUICE 的 ΔV_P1 = +0.107 mm/s 做加法修正。

**處理方式（已寫入 05a）：**
- 5.2 潮汐項：狀態記為「尚未重驗」，本次飛掠只作環境記錄，不產生量化預測，也不作為驗證依據。
- 5.3 ap：c₂ 狀態記為「待重驗」，本次只作記錄變數，不產生量化預測。但取值規則照常事前凍結：NASA OMNIWeb OMNI2 daily ap，2026-09-28 UTC 日值，與 `01_Observations.md` 的歷史資料一致；若分析時尚未更新，記為「待定」，不改用其他來源或時間窗。
- 第 6 節解讀規則：殘差 > 1 mm/s 時，潮汐與 ap 只做描述性比對，不做定量檢驗。
- **本條不修改** `02_Constraints.md`、`04_Mathematics.md`、`Appendix_Data.md` 的原有數值。這些文件的 c₂、26% 敘述應另行加註「審計前擬合、待重驗」，列入下方待補。

**影響範圍界定：**
- **不受影響：** `05_Predictions.md` 的 JUICE／Europa Clipper 封存值（ΔV_P1 由 Anderson P₁ 公式與 Horizons 角度直接算出，不依賴 c₂ 或潮汐項）；閉合軌道定理；factor of 2 的來源。
- **受影響：** 訊號階層「第二層 = ap、第三層 = 潮汐」的定量部分目前沒有可信係數，只剩定性排序，且定性排序本身也建立在審計前的資料上。

**方法論意義：** 這是 2026-07-02 原則的又一次延伸——不只數值要有可追溯的計算鏈，**擬合係數也要追溯它用了哪一版輸入**。上游資料修正後，下游的擬合結果不會自動失效，文件裡的數字卻會一直「看起來還是對的」。這次是在寫預先登記文件時，因為要把數字凍結下來，才被迫回頭檢查，進而發現。

**受影響文件：** `05a_JUICE_2026_Boundary_Conditions.md`、`EN/05a_JUICE_2026_Boundary_Conditions_EN.md`、CHANGELOG.md（本檔案）

---

## 2026-09-23 — 新增 05a（JUICE 預先登記邊界條件）、lunar_solar_geometry.py；results.json 新增日—地—月幾何

**起因：** JUICE 飛掠（近地點 2026-09-28 11:46 TDB）發生在滿月後約兩天。與 GPT 討論時提出：月相本身不產生額外引力，但日—地—月的幾何狀態應在飛掠**之前**凍結記錄為環境邊界條件，以免資料公布後才「事後找因素」。

**新增檔案：**
1. `05a_JUICE_2026_Boundary_Conditions.md`／`EN/05a_JUICE_2026_Boundary_Conditions_EN.md`：預先登記的環境條件、輔助項狀態與解讀規則。明確聲明**不修改** `05_Predictions.md` 的任何封存值，屬附屬登記，不是新預測。
2. `lunar_solar_geometry.py`：從 Horizons 原始檔計算近地點時刻的日—地—月幾何。重用 `reconstruct.py` 的 `parse_horizons_vectors()` 與 `find_precise_perigee()`，確保近地點判定與封存重建完全相同；遇到非地心或非 ICRF 的輸入時直接拒絕計算。
3. `data/raw/Moon_2026Sep_horizons_geocentric.txt`、`data/raw/Sun_2026Sep_horizons_geocentric.txt`：Horizons VECTORS，Center 500@399，ICRF 赤道面，1 分鐘步長，2026-09-24 00:00 至 2026-10-01 00:00 TDB。

**results.json 更新：** 新增 `JUICE_2026_boundary_conditions` 鍵，其他鍵不變。主要數值：
- 最近滿月：2026-09-26 16:50:48 TDB（幾何黃經差 = 180°）；近地點在滿月後 **42.92 小時**
- 日—地—月角距 156.683°，月相角 23.261°（兩者和 179.944°，自洽），月面照明 **0.9594**
- 地月距離 373,532.1 km
- 月球方向 vs. 近地點位置 121.485°（JUICE 在地球背月側）；太陽方向 vs. 近地點速度 8.861°（速度幾乎指向太陽）
- GMST 183.4798°，ERA 183.1372°
- 牛頓第三體潮汐加速度（瞬間值，僅作記錄）：合計 2.250×10⁻⁶ m/s²

**驗證：**
- 近地點時間、高度（8,645.5 km）、速度（13.1233 km/s）與 `reconstruct.py` 的封存重建值完全一致，確認三份輸入檔的時間格點對齊。
- 滿月時刻換算為 UTC 16:49:39，即越南時間 23:49，與 timeanddate 公布的時刻一致（外部獨立核對）。
- 腳本先以人工合成的 Horizons 格式資料測試，已知的滿月時刻被準確找回，寫入 results.json 時不影響其他鍵。
- ESA 官方文章（2026-09-21）：「within 8640 km」、11:45 UTC。與 Horizons 值差約 5.5 km、1 分鐘內，一致；ESA 數值為四捨五入。

**過程中發生的錯誤（誠實記錄）：**
1. **Moon／Sun 第一次下載用了黃道座標。** Horizons 的預設 reference plane 是 ecliptic，查詢時沒有手動改。`lunar_solar_geometry.py` 的 frame 檢查直接擋下，沒有產生錯誤數值。改用 `REF_PLANE='FRAME'` 重新下載後解決。
2. **第二次下載的檔案被截斷。** 從瀏覽器另存約一萬行的輸出時，Moon 停在 9/29 18:18，Sun 停在 9/26 16:06（連近地點都沒涵蓋），最後一行都只寫了一半。由於缺少 `$$EOE`，parse 階段直接報錯，同樣沒有產生錯誤數值。改用 `curl.exe` 直接下載後完整。
3. `data/` 根目錄下的 `JUICE_horizons_results.txt`、`JUICE_Earth_horizons_results.txt`（以及 Europa Clipper 對應檔）是太陽系質心、黃道座標、1 天步長的舊檔，**不屬於 pipeline 輸入**。本次正確使用的是 `data/raw/Juice_horizons_results_min.txt`。
4. `05a` 中文版的英文連結原本指向不存在的檔名（少了 `_EN`），已修正。

**AI 產生數值與 pipeline 數值的差異（方法論記錄）：** 最初提出滿月議題的 GPT 回覆中，引用了「月面照明約 98.6%」與「最近距離約 8,640.9 km」。pipeline 算出的照明比例是 **95.9%**（滿月後 43 小時，月球已移動約 23°，此值合理）；ESA 原文只寫「8640 km」，並沒有小數位。兩個數字都只出現在 LLM 文字中、查不到可追溯的來源，**均未採用**。這再次印證 2026-07-02 的原則：AI 可以參與推理（「滿月值得記錄」這個想法是有用的），但不能成為數值的來源。

**尚未完成：**
- `lunar_solar_geometry.py` 中的 GM_Moon、GM_Sun 暫時寫在程式內，標記為「待對照 DE440」，應移入 `constants.py`。
- 05a commit 後，README 連結改為指向該 commit hash 的 permalink，作為飛掠前登記的公開時間證據。

**受影響文件：** 新增 `05a_*.md`（中英）、`lunar_solar_geometry.py`、`data/raw/Moon_*`、`data/raw/Sun_*`；修改 `results.json`、`README.md`／`README_ZH.md`（新增 05a 連結）、CHANGELOG.md（本檔案）

---

## 2026-08-18 — 日期更新：系統時間確認為8月18日

**處理方式：延續2026-07-14建立的原則（見下方該條記錄）。** 不回頭修改7月2日~7月14日期間寫入的日期標籤——同樣原因：無法逐輪確認對話實際橫跨的真實時間分佈，強行「精確修正」個別歷史標籤會製造假精度。這些標籤之間的**先後順序**依然正確，只有絕對日期是近似值。

**往後所有新增內容一律使用系統真實日期（今日起為2026-08-18），不再自行遞增或沿用舊日期。**

---

## 2026-07-14 — reconstruct.py擴充：新增RA輸出、自動漸近段偵測；results.json新增7案RA數據

**新增功能：**
1. `ra_position()`、`ra_velocity()`：計算位置與速度方向的赤經
2. `find_asymptote_auto()`、`extract_asymptotes()`：自動偵測入射/出射漸近段收斂點（找逐日赤緯變化量最小處），取代先前逐案例肉眼判斷「哪天看起來穩定」的做法

**驗證：** 用新自動化流程重跑全部7案，δᵢ、δₒ與先前人工判斷值全部吻合（差異<0.15°，6/7案例達「high confidence」）。過程中Galileo I初次誤用精確近地點檔案(1分鐘/24小時)做漸近段偵測，因範圍不足被自動標記警告，換回正確的寬窗口檔案(1小時/2個月)後解決——此錯誤誠實記錄，未隱藏後直接呈現修正結果。

**用途：** 支援候選6第二層SO(2)不變量（I₂=磁軸、I₃=太陽風方向、I₄）的檢驗，這些量需要v̂的完整方向（RA+Dec），先前只存了δ不夠用。

**results.json更新：** 全部7案新增`ra_i_deg`、`ra_o_deg`欄位；I₂、I₃初步檢驗結果（負面，見07_Open_Problems.md）記入known_open_issues。

**修改的檔案：** `reconstruct.py`（新增4個函式，1個bug產生後立即修正——str_replace操作意外刪除`daily_declination_trend`函式定義行，已修復並驗證語法與執行正確）、`results.json`

---

## 2026-07-14 — 發現並記錄：文件內部日期標記不準確

**問題：** 使用者指出系統實際日期是2026-07-14，但這幾輪對話裡新增的文件段落（候選7重建、GEM推導A₂=0、候選6散射矩陣形式化等）標記的日期是07-01~07-03，是Claude在單一持續對話中把「對話輪次先後」誤當成「日曆日期流逝」，每輪自行遞增一天，未逐輪核對系統時間。

**影響範圍界定：**
- **不受影響：** 所有物理/數學內容本身——七案重建數字、候選7推翻結論、A₂=0推導、散射矩陣測試Juno失敗——不因日期標籤錯誤而改變
- **受影響但不嚴重：** 文件裡的日期標籤（版本標頭、🔴更新標記等）不是精確的日曆日期
- **未受影響：** 這些標籤之間的**先後順序**是正確的（候選7重建確實發生在GEM推導之前），只有絕對日期不準確

**處理方式（使用者選定）：** 不回頭修改過去已寫入的07-01~07-03標籤——因為無法確認對話實際橫跨的真實時間分佈，強行「精確修正」個別日期反而會製造假精度。保留原始標籤作為**相對順序記錄**，本條CHANGELOG說明其性質；**往後所有新增內容一律使用系統真實日期**，不再自行遞增。

**這件事本身的意義：** 這是這幾週建立起來的方法論原則（「任何數值/記錄都該有可追溯來源，不是LLM憑印象生成」）的又一次真實案例——這次出錯的不是物理數值，是時間戳記本身，同一套「發現→誠實記錄→不掩蓋」的處理方式仍然適用。

---

## 2026-07-03 — GPT跨模型review：全專案文件一致性稽核

**起因：** GPT review整個repo後指出「主文件尚未完全同步」——研究內容已經進步（GEM推導、七案重建），但部分文件仍殘留舊結論。Claude逐條核對後確認四項屬實、一項判斷不夠精確。

**確認為真、已修正：**
1. `02_Constraints.md` C2：Juno幾何預測仍寫+10.4mm/s → 修正為+6.0~6.3mm/s（用重建後角度，與Acedo 2017交叉驗證）
2. `02_Constraints.md` C4：A₂/A₁≈0.121仍列為觀測約束 → 標記推翻，加註GEM推導A₂理論值為零
3. `03_Hypotheses.md`：核心變量圖仍稱ap為「★最強信號」→ 移除此標註（原本支撐此判斷的P₂相關性已推翻，層級判斷目前未定）；P₂模式描述加註GEM限定
4. `05_Predictions.md`/`_EN.md`：缺少密封後有效性聲明 → 新增，明確列出候選7、|P₂|判準的前提已知污染，密封值本身不變但解讀需納入此事實
5. `CHANGELOG.md`本檔案：JUICE精確δ_peri待辦仍標未完成 → 已於results.json完成，勾選更新

**GPT判斷不夠精確、已釐清的部分：**
- 「04_Mathematics.md因子2未解」：實際上該節已有清楚的過時警示（2026-06-28即標註），非真正問題
- 「04_Mathematics.md P₂來自J₂」：該節文字方向其實是**反駁**J₂能解釋P₂（J₂比需要值小112倍），不是主張P₂來自J₂；真正該補的是這個112倍比較所依據的A₂/A₁=0.121本身已過時，已加註

**額外主動補強（review過程中一併發現）：**
- `04_Mathematics.md`「7.3倍放大」段落：內容本身無誤但已在後段被推翻，前段缺乏就地警示，讀者由上往下讀會先接觸到已被否定的數字——已補上就地警示

**方法論意義：** 這次驗證過程本身示範了專案既有原則——「跨模型討論用來找漏洞，不是用來背書真實性」。GPT的review沒有被照單全收，而是逐條核對後才採信，其中確實抓到一個真正的判斷失誤（P₂來自J₂的方向講反）。

**受影響文件：** 02_Constraints.md、03_Hypotheses.md、04_Mathematics.md、05_Predictions.md/_EN.md、CHANGELOG.md（本檔案）

---

## 2026-07-02 — 架構轉變：從「文字推理」改為「程式碼+版本記錄」

**起因：** 研究者用 Claude Sonnet 4.6 完成飛掠計算後，改用 Sonnet 5.0 review，發現前面的計算結果和數據對不起來。GPT review 後指出核心風險：「同一份推導，不同模型 review 後得到不一致結果」，且模型換版本可能偷改定義、補上假設、忽略前提，因此計算不該只存在於文字對話裡。

**實際查出的問題（比「換模型」更根本）：**

1. **δ_peri（近地點赤緯）在全部 7 個歷史飛掠中，從一開始就沒有留下任何計算方法的記錄**——不是被遺忘，是可能從未真正用 Horizons 原始向量算過。
2. **物理矛盾證據：** Galileo I 的舊 δ_peri=−61.8°，超出其軌道傾角 142.9° 允許的赤緯物理上限（37.1°），數學上不可能是赤道座標的位置赤緯。
3. **範圍比預期更大：** 重新用 JPL Horizons（赤道座標/ICRF，1分鐘步長精確近地點搜尋）逐一重建 7 個飛掠後，發現不只 δ_peri，**連 Juno 的 δᵢ、δₒ 本身也是錯的**——新值與獨立文獻（Jouannic et al. 2015、Acedo 2017）幾乎完美吻合，舊資料庫值是孤例。
4. 候選7拓樸判準（「7/7無自由參數分類成功」）、|P₂(cosδ_peri)|相關性（r=0.852，框架核心的「幾何為主導」論述）、RQ13的「Juno vs Cassini最強單一約束」、RQ15，全部建立在這批未經驗證的 δ_peri 上，逐一被推翻或降級為存疑。

**結論：** 問題的本質不是「哪個LLM算得比較準」，是**數值從一開始就沒有可重現、可追溯的計算鏈**，只要曾經被文字推理生成過一次，之後不管誰用什麼模型去複述，都可能延續同一個未被驗證的錯誤，或引入新的不一致。

**採取的行動：**
- 建立 `constants.py` 作為物理常數唯一權威來源
- 建立 `reconstruct.py`，把「從 Horizons 原始檔案算出 δᵢ/δₒ/δ_peri/近地點高度」的邏輯固定成程式碼，並在 parse 階段自動擋下已知會出錯的設定（地面觀測站當中心、非赤道座標）
- 建立 `validation_tests.py`，把這次手動抓出的錯誤模式（高度誤差過大、赤緯超出傾角物理上限、觀測值文獻分歧未標註）變成自動檢查
- 建立 `results.json` 作為數值的唯一權威來源，取代散落在各篇 .md 文件裡、可能互相不一致的數字
- **新規則：任何 LLM（不限模型或版本）之後只能 review 程式碼邏輯和 results.json 的內容，不可以用文字推理重新產生 δᵢ/δₒ/δ_peri 這類數值**

**驗證：** `reconstruct.py` 對 Galileo I 重跑，輸出與本次手動分析完全一致（近地點高度970.58km、位置赤緯23.8109°、速度赤緯−25.5786°），確認程式碼邏輯正確可信。`validation_tests.py` 對全部7案跑過，16項檢查全數通過。

**受影響文件：** 01_Observations.md/_EN.md、02_Constraints.md/_EN.md、04_Mathematics.md/_EN.md、07_Open_Problems.md/_EN.md、Appendix_Data.md/_EN.md 均已同步更新，標記舊值與新值、推翻與存疑的結論。

---

## 待補（下次進度）

- [ ] 將本次上傳的 7 份 Horizons 原始檔案正式歸檔到 `data/raw/`（本次因檔案上傳至對話環境、非持久化儲存，需使用者自行下載後放入該資料夾）
- [ ] Rosetta I 的 δᵢ 用更長查詢窗口重新確認（目前信心標註為低）
- [ ] Cassini 觀測值分歧（−2.00 vs −0.5±0.5 mm/s）尚待文獻定案
- [ ] P₂/P₃/P₄ 多極擬合係數：需要用全部7案新角度重新執行原始最小二乘擬合
- [ ] 潮汐項雙變量模型（`ΔV = 10.94 − 0.47·ap + 5.18×10¹³·T_proj`）用修正後資料重驗（見 2026-09-23 負面發現）
- [ ] ap 係數 c₂ 用修正後的 ΔV_Anderson 重新擬合，並與 −0.47 的分歧統一說明（見 2026-09-23 負面發現）
- [ ] `02_Constraints.md`、`04_Mathematics.md`、`Appendix_Data.md`（含 EN 版）中的 c₂ = −0.249 與「潮汐 26%」加註「審計前擬合、待重驗」
- [ ] GM_Moon、GM_Sun 對照 DE440 文件後移入 `constants.py`
- [ ] JUICE 飛掠後：依 05a 第 6 節規則比對官方 OD 結果，結果另寫一份文件，不修改 05a
- [x] JUICE/Europa Clipper 的精確 δ_peri（1分鐘步長）：Europa Clipper已完成（2026-07-02），JUICE已完成（2026-07-03，見results.json，近地點時刻2026-Sep-28 11:46 TDB，高度8645.5km）
